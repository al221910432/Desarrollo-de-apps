/*
Durante el desarrollo, use una declaración de aserción
 - ; - para interrumpir la ejecución normal si una condición booleana es falsa.
*/
void main() {
  // Make sure the variable has a non-null value.
assert(text != null);

// Make sure the value is less than 100.
assert(number < 100);

// Make sure this is an https URL.
assert(urlString.startsWith('https'));
}